import React, { FC } from 'react';

interface AboutUsProps {
    header: string;
};

const AboutUs: FC<AboutUsProps> = ({ header}) => {
    return (
        <>
        </>
    );
};

export default AboutUs;