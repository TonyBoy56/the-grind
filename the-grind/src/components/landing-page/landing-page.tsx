import React, { FC } from 'react';

interface LandingPageProps {
    header: string;
};

const LandingPage: FC<LandingPageProps> = ({ header}) => {
    return (
        <>
        </>
    );
};

export default LandingPage;